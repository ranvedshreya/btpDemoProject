_schema-version: "3.2"
ID: salesincentive
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: salesincentive-destination-content
  type: com.sap.application.content
  requires:
  - name: salesincentive-destination-service
    parameters:
      content-target: true
  - name: salesincentive-repo-host
    parameters:
      service-key:
        name: salesincentive-repo-host-key
  - name: salesincentive-uaa
    parameters:
      service-key:
        name: salesincentive-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: salesincentive_html_repo_host
          ServiceInstanceName: salesincentive-html5-service
          ServiceKeyName: salesincentive-repo-host-key
          sap.cloud.service: salesincentive
        - Authentication: OAuth2UserTokenExchange
          Name: salesincentive_uaa
          ServiceInstanceName: salesincentive-xsuaa-service
          ServiceKeyName: salesincentive-uaa-key
          sap.cloud.service: salesincentive
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: salesincentive-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: salesincentive-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - salesincentive.zip
      name: salesincentive
      target-path: resources/
- name: salesincentive
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: salesincentive-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: salesincentive-destination-service
    service-plan: lite
- name: salesincentive-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: salesincentive-xsuaa-service
    service-plan: application
- name: salesincentive-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: salesincentive-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
